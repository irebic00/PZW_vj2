<UserControl x:Class="PZW_Lab2.Controls.Post"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:PZW_Lab2.Controls"
             mc:Ignorable="d" x:Name="PostElement"
             d:DesignHeight="148" d:DesignWidth="500">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <controls:User x:Name="CurrentUser" Margin="10" Title="{Binding ElementName=PostElement, Path=user}"></controls:User>
            <TextBlock x:Name="userspost" VerticalAlignment="Center" Margin="10" FontSize="20" Text="{Binding ElementName=PostElement, Path=post}"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
